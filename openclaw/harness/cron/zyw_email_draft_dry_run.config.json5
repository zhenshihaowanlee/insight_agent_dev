{
  // Template only. Enable only after local runtime-guard passes.
  name: "zyw-72h-local-draft-artifact",
  enabled: false,
  mode: "dry_run",
  command: [
    "python3",
    "-m",
    "zyw_insight.cli",
    "email-draft",
    ".zyw_insight/runs/<run-id>-72h-dry-run"
  ],
  environment: {
    PYTHONPATH: "src"
  },
  output: {
    artifact_mode: "draft_only",
    requires_human_approval: true,
    external_delivery_sent: false
  },
  runtime_boundary: {
    local_artifact_only: true,
    network_actions_allowed: false,
    provider_credentials_allowed: false,
    external_delivery_allowed: false
  }
}
