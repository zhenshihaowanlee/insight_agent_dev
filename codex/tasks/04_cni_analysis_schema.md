# Task 04 — CNI analysis schema and renderer

Goal: ensure analysis outputs validate against schema and render to Markdown.

Requirements:

- Add `render_analysis_markdown`.
- Include all 20 CNI sections.
- Include Network Impact Vector.
- Include score and recommended action.

Acceptance:

- Example JSON validates with local checks.
- Markdown contains all required headings.
